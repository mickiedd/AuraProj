# 2026-09-24

## Zhengximen / Great West Gate — package received, import BLOCKED

User supplied `Zhengximen_GreatWestGate_UE5_Package.zip` (244 MB, 93 files, unpacked
293 MB) and asked for it to be imported as a building Blueprint. **Not started — the
macOS boot volume is full.**

### Environment blocker (important, will recur)

`df -h`: `/System/Volumes/Data` = **197 Gi used, 183 Mi available, 100% full**.
Consequence: the agent sandbox cannot create its SBPL temp file under
`/var/folders/ns/.../T` (`os error 28, No space left on device`), so **every shell
command fails** — including `grep`/`Glob`-via-ripgrep. Only the `Read` tool works.
The failure is intermittent (a few commands succeeded early in the session), so a
retry loop is worth one attempt before declaring the environment dead.

Two consequences for planning:
- `/tmp` lives on the full volume — **never extract packages there**. Stage under
  `/Volumes/M2` (374 Gi free).
- Unreal writes DDC/temp constantly; a 100%-full boot volume will also destabilise
  the shared editor, not just the agent shell.

### Package contents (read from the zip while shell briefly worked)

`Zhengximen_GreatWestGate_UE5/` — self-describing, same vendor/shape as the
already-handled `Raw3DPacket/Zhenhai_Tower_UE5_UltraDetail`:
- `Meshes/` LOD0–LOD4 in **both** GLB and FBX; LOD0 FBX has 5 UCX proxies, sockets
  (`SOCKET_GateCenter/PlayerEntry/LeftWall/RightWall/RoofTop`) and a second
  non-overlapping **UV1 lightmap atlas**; GLBs carry only UV0.
- `Meshes/Modular/` — 7 per-material-group GLBs (GrayBrick, StoneFoundation,
  AgedWood, DarkTimber, ClayRoofTile, LimePlaster, BlackIron, GatePlaque).
- `Textures/` — 48 × **4096×4096** PNG: BaseColor, Normal (DirectX/Y-), Roughness,
  Metallic, AO, Height per material. Only BaseColor is sRGB.
- `Materials/` — `M_Zhengximen_Master.json` + 8 `MI_*.json` specs.
- `Unreal/Import_Zhengximen_UE5.py`, `Unreal/Build_Zhengximen_Materials_UE5.py`.
- `Documentation/` — `AssetManifest.json`, `ValidationReport.json`, `SHA256SUMS.txt`.

Stats: HighPoly 421,432 tris; LOD0 206,252 → LOD4 484. LOD0 bounds 53.2 × 10.8 ×
16.9 m (cm in FBX, m in GLB). Stated dims: wall 25 m, span 53 m, depth 10 m,
roof 16.4 m, gate opening 4.5 m wide / 5.65 m arch. Scale is an art reconstruction,
not a survey.

### Project state verified

- Showcase has **7** landmarks, all `BP_*` PackedLevelActors:
  Zhengnanmen_HighFidelity, Xiaobeimen_AAA_V3, Guidemen_ReferenceRepaired,
  Wuxianmen_V5_FullPBR, GreatNorthGate, ZhenhaiTower, Wenmingmen.
- **Zhengximen is not among them** → this is the 8th landmark.
- `Content/Assets/Environment/GuangzhouLandmarks/` has **no GreatWestGate folder**.
- Stale `Scripts/{ImportAndPlace,Wrap,Capture,Validate,Inspect}GreatWestGate*.py` exist
  but target a **different source** (`C:/Works/Raw3DModels/SM_Zhengximen_GreatWestGate_5M_PBR.glb`,
  ~5.7 M tris, 20 parts) and use **Windows absolute paths** (`C:/Git/AuraProj`). They
  do not match this package (LOD chain + modular groups + 48 textures) and are not
  reusable as-is.

### Plan once disk space is freed

1. Extract to `/Volumes/M2/Works/AuraProj/Raw3DPacket/Zhengximen_GreatWestGate_UE5/`.
2. Import **LOD0 FBX** (UV1 + collision + sockets) into
   `/Game/Assets/Environment/GuangzhouLandmarks/GreatWestGate/`; Nanite ON, LOD1–4 as
   authored LODs. 48 textures + `M_Zhengximen_Master` + 8 `MI_*`.
3. Wrap as `BP_GreatWestGate` — PackedLevelActor + one HISM component per source mesh,
   per `WrapLandmarkMeshBlueprints.py` / `CreateGreatSouthGateActorAsset.py`.
   `compile_blueprint` returns `None` (void) — verify by spawning the class.
4. Add to `LANDMARKS` + `EXPECTED_LABELS` in `CreateGuangzhouLandmarkShowcase.py`
   (light label derives from the latter), rebuild, then validate → expect 0/0.
5. Archive: SVG diagram + MD record in `Docs/Reports/Change-Archive/`, append to
   `.claude/memory/visual-change-archive.md` (append-only, union on conflict).

### Concurrency note

A second agent was live in the shared editor during this session — Wenmingmen probe
logs at 23:50–23:53 in `Saved/RawModelImport/`. Serialize heavy passes; confirm
`get_dirty_content_packages()` and `get_dirty_map_packages()` are empty before
switching levels.
