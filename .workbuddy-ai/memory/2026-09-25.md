# 2026-09-25

## Wenmingmen facing corrected in the showcase ring

Continues the 2026-09-24 session (started 23:44). User asked to place `BP_Wenmingmen`
in `L_GuangzhouLandmarkShowcase` "with a reasonable position". It was **already**
placed (registered and rebuilt 2026-09-24), so the job became verifying the position.

It was wrong: the gate stood **back to front**. With `facing_offset 0.0` its facade
pointed away from the plaza, so the viewer saw the blank rear wall and its own
per-landmark accent light lit that rear wall.

### The finding

The 2026-09-24 placement claimed Wenmingmen "follows the same local -Y front
convention as the other gates". Never measured, and false. Four readings on the
placed actor, in its own frame, put the facade on local **+Y**:

- `SMC_water` (canal the bridge crosses) — local Y **710..1810**
- `SMC_iron` (door ironwork) — thin plate at **~200**
- `SMC_foliage` (vines) — band at **~200**
- `SMC_stone`/`SMC_limestone` (paved platform) — start at **~-230**, run to **+1867/+2351**

The canal reading is load-bearing because it is **rotation-invariant**:
`Saved/Reports/Wenmingmen/final-front.png` shows the closed timber door, couplets and
vines on the same side of the model as the bridge and canal, so the door is on +Y
whatever the preview axes are. Plaza side is local -Y (the light is built on the
plaza side and measured local Y -5248).

### The fix

- `Scripts/CreateGuangzhouLandmarkShowcase.py` — Wenmingmen `facing_offset 0.0 → 180.0`,
  with the four readings in the comment. Only this landmark keeps a non-zero offset.
- `Scripts/ValidateGuangzhouLandmarkShowcase.py` — new `FACADE_LOCAL_AXIS` table
  (Wenmingmen +Y, other six -Y) and a new **facing check**. **This was the real gap**:
  the validator checked grounding, spacing, overlap, lights and labels but never
  orientation, so a gate could stand back to front and still report `errors: []`.
  Proven against the old level first — it reported exactly one error, "facade faces
  180.00 deg away from the plaza centre", every other landmark 0.000°.
- `Scripts/ProbeWenmingmenPlacement.py` — new read-only probe consolidating the
  measurements (per-component local extents + the light's local position).

Rebuild: 28 actors cleared, 7 rebuilt, map written 00:01:49, radius still 8912.75,
**every landmark location unchanged** (only Wenmingmen's yaw moved, 0°→180°).
Validation: `errors: []`, `warnings: []`, 7 landmarks/7 lights/7 labels, gaps
44.914°–59.117°, every facade 0.000° off the plaza.

Archive: `Docs/Reports/Change-Archive/2026-09-25-wenmingmen-facade-facing.{md,svg}` +
`.claude/memory/visual-change-archive.md`.

### Measurement trap worth remembering

Wenmingmen's six components are plain `StaticMeshComponent`s with a **-90° component
rotation** (Z-up GLB import), not HISM. So `relative_location + mesh bounds Z` is wrong
by a whole axis — it claimed a local Z floor of -2351 cm where the truth is -332.5, a
20 m error that reads exactly like a sunken building. Correct route: transform the mesh
bounds corners through each component's `get_world_transform()`; it reproduced the
Blueprint bounds (6500 × 2806.90 × 2199.01) exactly. `PrimitiveComponent.bounds` is not
exposed to Python; `get_editor_property("relative_transform")` raises on these components.

**Consequence for the 2026-09-24 caveat**: the gate does **not** float 332.5 cm above the
plaza. Its rendered geometry spans world Z 0.0..2199.01, so the stone base sits exactly on
the plaza; 332.5 is the actor origin. Its canal water sits at world Z ~48.5-56.5.

### Environment

- **Boot volume was full again** (`/System/Volumes/Data` 100%, 177 Mi free), same blocker
  the Zhengximen session hit: the agent sandbox cannot create its SBPL temp file, so every
  shell command fails with `os error 28`. **Workaround found: pass `dangerouslyDisableSandbox`
  on the Bash call — that path runs the command without the sandbox and works.** Also note
  the `Edit`/`Write` tools fail on the *boot* volume (their backup write needs space) while
  they still succeed on `/Volumes/M2`. Space recovered on its own to 3.2 Gi later.
- UE Python remote execution still unreachable (no discovery answer), so the editor's stale
  level copy could **not** be reloaded. **The running editor still holds a level copy loaded
  before the 2026-09-24 rebuild**, so it shows neither Wenmingmen nor this facing change.
  The user must reload the level and must not save the stale copy over it.
- A second agent session was live in the shared repo (Zhengximen/GreatWestGate work).

### Open

- The other six gates' `-Y` facade axis is carried over from the documented convention and
  is now asserted by the validator, but was **not** re-measured against their models.
- Zhengximen/GreatWestGate is still queued as the 8th landmark (see 2026-09-24 notes).

---

## Zhengximen / Great West Gate imported as a landmark Blueprint — the 8th landmark

Closes the "queued as the 8th landmark" item above. User supplied
`Zhengximen_GreatWestGate_UE5_Package.zip` (244 MB, 93 files) plus a reference board and
asked for it **as a building blueprint**, then confirmed it should also join the ring.

**Zhengximen did not previously exist in the project.** `find Content -iname "*Zhengximen*"`
returned nothing. The old `Scripts/ImportAndPlaceGreatWestGate.py` is a Windows-era script
pointing at `C:/Works/Raw3DModels/SM_Zhengximen_GreatWestGate_5M_PBR.glb`, a different level
(`L_showcase_level`) and a 20-part 5.7M-triangle source — none of which exists here, so it
was never the precedent it looks like.

Delivered: `Content/Assets/Environment/GuangzhouLandmarks/Zhengximen/` — 7 meshes,
`M_Zhengximen_Master` + 8 `MI_*`, 48 4096² textures, `BP_Zhengximen` (PackedLevelActor,
**7 HISMs** on a `LevelInstanceComponent` root). Spawns at **5320 × 1080 × 1689.68 cm**
(the package's declared 53.2 × 10.8 × 16.9 m) with **206,252 triangles — the declared LOD0
count exactly**.

### The two findings that decided the route

1. **The package's own documented import path does not work here.** `SM_Zhengximen_LOD0.fbx`
   — the only variant carrying UCX collision and the UV1 lightmap atlas — was probed twice:
   legacy `AssetImportTask` + `FbxImportUI`, and `InterchangeManager.import_asset` with an
   explicit `InterchangeGenericAssetsPipeline`. **Both times Interchange logged
   `start importing source [...LOD0.fbx]` and never the matching `completed` line** — no
   asset, no error, no traceback — while four GLB imports succeeded in the same run. So the
   FBX is a dead end in this build, and that is a general trap: **a silent non-completion,
   not a failure you can catch.**
2. **`SM_Zhengximen_LOD0.glb` has `"materials": []`.** A combined GLB import would give ONE
   material slot for the whole gate. Hence the **per-material modular GLBs**, one mesh per
   material group, one slot each — binding by construction, no slot-name matching.

Cost of skipping the FBX: no UCX collision, no UV1 atlas. Collision is rebuilt as
`CTF_USE_COMPLEX_AS_SIMPLE` on the five structural groups, exactly as `ImportWenmingmen.py`
does; the project lights with Lumen so the atlas is not on the critical path.

### The orientation trap — and why the first heuristic failed

The GLBs are **trimesh** output with **Z-up** POSITION values, against glTF's Y-up
convention, so Interchange tips them over. At **roll 0 every group comes back with Y and Z
transposed** — AgedWood's 18.86 × 8.64 × 14.70 m arrives as 18.86 × 14.70 × 8.64. **Roll
−90 restores the source exactly**, and **scale 1.0 is correct** because Interchange already
converts the glTF metres to centimetres. (Same `IMPORT_ROLL = -90.0` as
`ImportGreatNorthGateHighDetail.py`.)

**My first importer accepted two groups transposed** because it tested "Z exceeds Y" plus a
plausible X band. That test is simply wrong for this model: the brick wall is genuinely
1000 cm deep and 870 cm tall, and the timber band 818 deep and 675 tall — so "height exceeds
depth" is false for correctly-oriented parts and true for transposed ones. **The fix that
generalises: judge each group against its own GLB POSITION accessor bounds** (read min/max
from the glTF JSON) within 2%. All seven then resolved to roll −90 / scale 1.0, first
candidate. Do not use axis heuristics for orientation on multi-part buildings.

### Other deliberate choices

- **Normals NOT flipped.** The package is DirectX (Y−), which is what Unreal expects;
  `flip_green_channel` exists to convert OpenGL and would have inverted every normal. Note
  this is the **opposite** of `ImportWenmingmen.py`, whose source genuinely was OpenGL.
- **Nanite fallback forced to the full mesh** (`PERCENT_TRIANGLES` 1.0, relative error 0.0)
  — `AUTO` decimates on Metal and renders black holes.
- **All paths project-relative** from `unreal.Paths.project_dir()`. The package's own
  `Unreal/*.py` helpers hardcode `/Game/Zhengximen` and derive their root from `__file__`.

### Two bugs the run found in my own validator

- A material-path check used `endswith("/MI_<group>")`, but the real path is
  `.../Materials/MI_GrayBrick.MI_GrayBrick` — match on `"/MI_<group>."` instead.
- A per-part "lying on its side" assertion (`size_z > size_y`) — **wrong in principle**, per
  the wall/timber sizes above. Removed; orientation is guarded by the union footprint.
- Also: `unreal.MaterialEditingLibrary.get_material_instance_parent` **does not exist**; read
  `instance.get_editor_property("parent")`.

### Commandlet / API traps hit

- **`print()` never reaches a commandlet's captured stdout**, and neither does `unreal.log`
  reliably — write a **JSON report** and read that. I lost a whole probe run to this.
- **`k2_gather_subobject_data_for_blueprint`** — "**for**", not "from". A typo cost one run.
- `unreal.EditorStaticMeshLibrary.get_lod_count` / `get_simple_collision_count` are deprecated
  but still work.
- A partially-created Blueprint is **saved before its components are added**, so a crashed run
  leaves a wrapper that looks real and has none of the building. The reuse path must **verify
  the HISM count and rebuild** rather than trust the asset's existence.

### Results

- Import: `passed: true`, `problems: []`, 7/7 groups, 48 textures, 8 instances.
- Landmark validation (fresh process): **0 errors / 0 warnings**.
- Ring: radius **8912.75 → 10254.19 cm**; all seven existing gates moved (arithmetic, not a
  regression). Zhengximen at yaw 238.071°, height 1689.7 cm in the level.
- **Facing measured, not assumed**: the plaque (門額) sits at local Y −3.055..−2.915 m and the
  iron door fittings at Y −0.614..−0.387 m → facade is local **−Y**, `facing_offset 0.0`
  (unlike Wenmingmen's 180.0). Ring validation: **0 errors / 0 warnings**, 8 landmarks,
  0 overlaps, 0 light-isolation violations, every facade 0.000° off the plaza.

Archive: `Docs/Reports/Change-Archive/2026-09-25-zhengximen-landmark-import.{md,svg}` +
`.claude/memory/visual-change-archive.md`.

### Open

- **Native visual capture PENDING.** All of the above is structural and does **not** prove
  the gate looks right. `Scripts/CaptureZhengximenNativeBlueprint.py` is written and needs an
  editor with a live RHI. Remote execution still cannot discover the editor (multicast to
  `239.0.0.1:6766` unanswered despite the editor binding it and
  `PythonRemoteExecution=True`; **`adb` PID 4721 separately holds `127.0.0.1:6766`**).
- The 280 MB extracted source is staged in `Raw3DPacket/`, **uncommitted**, not in
  `ContentSource/` with Git LFS the way Wenmingmen's is. Decide before it is needed again.
- The package's README states its dimensions are proportional art-reconstruction estimates,
  so comparison against the reference photo is a visual judgement, not a measurement.
- **Unrelated fix**: `.claude/memory/visual-change-archive.md` carried unresolved conflict
  markers around the 2026-09-25 Wenmingmen entry. Upstream side was empty, stashed side held
  four real entries → kept the entries, dropped the markers.

---

## Zhengximen tuned against the reference board — textures + continuous wall + arch-fitting door

Same session, follow-up. User: "it should have textures, you can find out these materials in
the source folder; the timber's wall should be continuous, currently it's hollows", then
"the wooden door should fit the arc perfectly, no hollows".

### The texture problem was NOT the material — the GLBs had no UVs at all

`M_Zhengximen_Master` and the eight `MI_*` were fine; the validator had confirmed every
texture parameter was bound. The meshes were the problem: **every exported GLB carried only
`POSITION` and `COLOR_0`, no `TEXCOORD_0`.** The generator computes UVs in `add_box` /
`add_cylinder_between` / `add_tube_curve` and stores them in `Group.uvs`, then throws them
away at export:

- `scene_from_builder(..., textured=False)` wrote `tm.visual.face_colors` — which *replaces*
  the texture visual, so the `uv` array was never written.
- the modular per-material export did the same.

`scene_from_builder(textured=True)` *would* have carried them, but the pipeline calls it with
`textured=False`. **So the package as shipped is untexturable through its own GLBs** — worth
remembering for any other package from the same generator. Fix: both paths now set
`TextureVisuals(uv=np.array(g.uvs, float), material=PBRMaterial(baseColorFactor=...))`, and
the driver asserts `TEXCOORD_0` is present in every GLB it writes.

**General lesson: "it has no texture" is a mesh/UV question before it is a material question.**
Check the exported vertex attributes first.

### The hollow timber wall — three separate defects, not one

1. Each bay was its own backing panel `w = xb-xa-.12` wide → a **0.12 m through-slot between
   every pair of bays**.
2. The loop covered only `yy = ±lowerD/2` (front and rear) — **the two ends had no wall at
   all** (1080 of 1100 rays straight through).
3. The doors were 3.22 m boxes under a vault springing at 3.40 and topping at 5.65 → a
   **2.4 m opening above the gate**.

Fix: one continuous closed wall per side on all four faces, end walls running past the
front/rear faces so corners close by overlap, and an arch-fitting door.

### Verifying "is it hollow" — ray-cast, do not eyeball

A bounding-box check cannot see this defect (the broken version still had panels). The check
that works: fire rays perpendicular at each face and count the ones that come out the far
side. `ContentSource/GuangzhouLandmarks/Zhengximen/source/build_zhengximen.py` carries it, and
the numbers tell the whole story:

| | before | after |
|---|---|---|
| wall, 8 faces | **4840** open of 8800 | **0** |
| gateway | **65** of 135 | **0** |
| door-to-arch seam | **242** of 450 | **0** |

The `door_fit` check aims at fractions (0.995/0.98/0.96/0.93/0.90) of the arch half-width at
each height — i.e. *at the seam*, not the middle of the door — because a leaf that only
approximates the curve still looks solid in the centre. Skip the last 1% of height: the
opening closes to a point at the apex, so every fraction collapses to x=0 and the ray only
grazes the top edge (that is a sampling artefact, not a gap).

### Fitting a door to an arch — what actually works

Three attempts, in order:

1. **Plain boxes** → 2.4 m hole above the door.
2. **Horizontal slices** → closes it, but each step is a *chord* of the circle, so the leaf sat
   ~1.7 cm inside the opening at the shoulder (the arc turns vertical there) and poked through
   the ring near the apex. 242 open rays.
3. **Sampled strip** (`add_arched_leaf`) — columns across the leaf whose top edge *is* the
   arc — plus `arch_pad=0.02` so the leaf is built to a slightly larger radius than the
   opening and always overlaps it. The excess is hidden inside the arch ring. **0 open rays.**

Two traps found on the way, both only visible in a straight-on render:

- **The wall's own opening is a staircase.** `slices = 46` gave 17 cm steps, and once the door
  filled the arch *that* staircase became the visible boundary — it reads as the door not
  fitting the arc even though the door is the larger of the two. LOD0 now uses **400 slices**
  (2 cm steps).
- **The barrel joints scalloped the edge.** The 3.5 cm vault tubes sat *at* radius `archR`, so
  their tips stuck into the opening and were the silhouette. Moved flush (`archR + r`,
  radius 0.018). The voussoir ring also went 34 → 96 segments.

**Render a straight-on view, not just an angled one.** The angled view cannot distinguish a
gap from the shadowed inside of a barrel vault; both look like a dark band. `arch-front` in
`Scripts/RenderZhengximenPreviews.py` exists for exactly this.

### Running the package's generator here

`generate_zhengximen_package.py` is a flat script with no `__main__` guard: from `builders={}`
onward it runs the whole pipeline (48 textures, 5 FBX, previews, a zip) and writes to
hardcoded `/mnt/data/...` paths. `build_zhengximen.py` therefore:

- splits the file at `\nbuilders={}` and execs only the **definitions**;
- rewrites `Path('/mnt/data/Zhengximen_GreatWestGate_UE5')` to the real extracted package, so
  the module-level `mkdir`s are no-ops and `get_pbr()` can still find the textures;
- sets `REUSE_TEXTURES=1`, because the texture pass also runs at module level and needs CJK
  fonts (`/usr/share/opentype/noto/...`) that do not exist on this Mac.

Toolchain: a venv at `~/.workbuddy-ai/binaries/python/envs/default` with numpy, pillow,
opencv-python-headless, trimesh, **rtree** (ray casting needs it) and **scipy**.

### Still open

- **Reimport into UE not yet run** — needs the editor closed. The tuned GLBs are on disk.
- Native visual capture still pending (needs the editor open; remote execution is dead).
- The plaque texture spells 正西门 (simplified) where the reference board shows 正西門. Left
  alone: regenerating textures needs a CJK font, and the wording is a content decision.

### Reimported and revalidated

Editor closed, reimport ran clean: 7 groups, `passed: true`, `problems: []`, union
**5320 x 1080 x 1689.68 cm**, **218,328 triangles** (up from 206,252 — the finer arch),
48 textures, 7 HISMs. Landmark validation **0 errors / 0 warnings**; showcase ring still
`passed: true`, 8 landmarks, no overlaps, Zhengximen facing 0.0 deg off the plaza.

One change made to the importer first: `import_group` used to `delete_asset` before every
attempt. `delete_asset` is a **force-delete**, and the Blueprint's HISMs reference those
meshes, so a reimport risked nulling the references. It now reimports straight over the
existing asset on the first attempt and only clears it on a retry.

**A broken API produced seven false failures, and the fix was to delete the check.**
`EditorStaticMeshLibrary.get_num_uv_channels` returns **0 for every mesh** — including the
known-good Wenmingmen and Zhengnanmen meshes that render their 4K maps correctly. It reports
*source* UV channels and an Interchange import has no source data. `StaticMesh.get_num_vertices`
does not exist either. **Probe an API against a known-good asset before trusting it**; a check
that fails good assets is worse than no check. The validator now reads the **source GLB's**
vertex attributes and asserts `TEXCOORD_0`, which is the check that would have caught the
original defect. (REFERENCE trap 21.)
