# Landscape extent check — 2026-09-24

**Blocked.** The provisional 2017-vertex, 2 m grid spans 4032 × 4032 m. There is no approved projected wall polygon, so its bounding box and the required 200 m margin cannot be calculated. No `Wall.geojson`, gate, road or waterway geometry is published because digitizing from an unaccepted map transform would imply false coordinate precision. Recompute the envelope and resize the grid if measured bounds exceed 4032 m.
